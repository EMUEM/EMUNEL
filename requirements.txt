# EMUNEL — unified service requirements (API + Core runtimes share this env:
# the InstanceManager spawns Core subprocesses with the same interpreter).

# --- shared runtime ---
fastapi>=0.110
uvicorn[standard]>=0.30
websockets>=12.0
cryptography>=42.0
psutil>=5.9
httpx>=0.26

# --- console API ---
sqlalchemy[asyncio]>=2.0.25
asyncpg>=0.29
aiosqlite>=0.19
pydantic>=2.5
pydantic-settings>=2.1
python-jose[cryptography]>=3.3
bcrypt>=4.1
python-multipart>=0.0.6

# --- tests ---
pytest>=8.0
pytest-asyncio>=0.23
