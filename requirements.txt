# EMUNEL — unified service requirements (single source of truth).
#
# The API and every Core instance subprocess share this environment:
# the InstanceManager spawns Core subprocesses with the same interpreter.
# NOTE: this file is a strict SUPERSET of core/requirements.txt (which is
# kept only for standalone Core usage); keep the pins compatible when
# editing either file.

# --- shared runtime (API + Core) ---
fastapi>=0.141
uvicorn[standard]>=0.30
websockets>=12.0
cryptography>=42.0.0
psutil>=5.9.0
httpx>=0.26

# --- console API ---
sqlalchemy[asyncio]>=2.0.25
asyncpg>=0.29
aiosqlite>=0.19
pydantic>=2.5
pydantic-settings>=2.1
python-jose[cryptography]>=3.3
bcrypt>=4.1
python-multipart>=0.0.6

# --- tests ---
pytest>=8.0
pytest-asyncio>=0.23
